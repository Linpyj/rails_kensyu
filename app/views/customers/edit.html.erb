<h1>商品編集ページ</h1>
<%= form_for @customer, url: {controller: 'customers', action: 'update', id: @customer.id}, html: {method: 'PUT'} do |f| %>

  <p>商品名<%= f.text_field :name %></p>
  <p><%= f.submit  %></p>
<% end %>

<%= link_to("顧客一覧へ",customers_index_path) %>